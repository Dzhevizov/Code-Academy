#include <stdio.h>

int iz4istiBitna(int x,int index);

int main(void){
    
    int rez=iz4istiBitna(15,3);
    printf("%d\n", rez);

    return 0;

} 

int iz4istiBitna(int x,int index){
    int mask=1;
    mask=mask<<index;
    mask=~mask;
    int y=x&mask;
    return y;
}
