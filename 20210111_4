#include <stdio.h>

int vdigniBitna(int x,int index);

int main(void){
    
    int rez=vdigniBitna(10,2);
    printf("%d\n", rez);

    return 0;

} 

int vdigniBitna(int x,int index){
    int mask=1;
    mask=mask<<index;
    int y=x|mask;
    return y;
}
