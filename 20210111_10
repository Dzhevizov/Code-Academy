#include <stdio.h>

int nullAfterBit(int x,int index);

int main(void){
    
    
    int rez=nullAfterBit(200,5);
    printf("%d\n", rez);

    return 0;

} 

int nullAfterBit(int x,int index){
    int mask = 4294967295;
    mask=mask<<index;
    mask=~mask;
    int y=x&mask;
    return y;
}
