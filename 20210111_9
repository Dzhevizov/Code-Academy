#include <stdio.h>

int nullPos(int x);

int main(void){
    
    
    int rez=nullPos(4095);
    printf("%d\n", rez);

    return 0;

} 

int nullPos(int x){
    int mask = 248;
    mask=~mask;
    int y=x&mask;
    return y;
}
